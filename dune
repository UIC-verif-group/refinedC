; Add project-wide flags here.
(env
 (dev
  (binaries (tools/rocq_timing.sh as rocq))
  (flags :standard))
 (release
  (binaries (tools/rocq_timing.sh as rocq))
  (flags :standard)))

(install
 (files FAQ.md ANNOTATIONS.md)
 (section doc)
 (package refinedc))
